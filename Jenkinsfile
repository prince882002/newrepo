properties([parameters([password(defaultValueAsSecret: <object of type hudson.util.Secret>, name: 'pwd'), string(defaultValue: 'password', name: 'pwd name', trim: true)])])
node {  
    stage('Build') { 
        echo "this is ${params.KEY}"
    }
}
    
